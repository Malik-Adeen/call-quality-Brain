---
tags:
  - log
---

# LOG — Daily Session Notes

> One line per session. 30-second ritual. Saves 10 minutes next session.

---

2026-06-30 — Codebase audit snapshot written to 66_Codebase_Audit_Snapshot.md (1480 lines). Full repo tree with line counts, all deps, all Dockerfiles, all docker-compose variants, nginx.conf, 6 invariant-critical source files, UploadCall.tsx + auth store + axios client, zero TODO/FIXME/HACK found, CI wired. Notable findings: (1) HIGH — ci.yml triggers on `main` branch but repo is `master`; push CI never fires on master commits. (2) MEDIUM — base Dockerfile CMD has `--reload` (dev mode); prod compose overrides it but the image default is unsafe. (3) MEDIUM — celery_app.py task routes use prefix `pipeline.tasks.*` but tasks register as `app.pipeline.tasks.*` — verify routing resolves correctly in prod. (4) LOW — docker-compose.hybrid.yml has hardcoded Windows paths and no REDIS_PASSWORD. (5) INFO — no frontend tests exist. Pytest collection fails on host (expected — must run inside cq_api container). .git size: 5.2M (lean). Working tree has ~70 deleted graphify-out/cache/ast/*.json files (GitNexus re-index in progress).\n\n2026-06-28 — Fixed public URL + startup automation on Fahad's machine. Dynu DDNS retired. Tailscale Funnel (`--bg 80`) is now canonical public URL: https://deltaroot-pt-lp.tailb8d983.ts.net/. CORS updated in .env.prod. netsh portproxy port 80 removed (conflicts with Docker Desktop). nginx must be --force-recreate on reboot (stale WSL2 bind mounts). Startup script (start-callquality.ps1) on Fahad's desktop. Task Scheduler fires at login with 2min delay. sudoers configured for passwordless docker/service. Handoff: 65_Session_Handoff_2026-06-28.md.

2026-06-25 — Fahad demo deployment complete on Windows laptop (RTX 5060 Blackwell, 64GB RAM, Docker Desktop + WSL2). Pipeline fully working — all 7 stages, Score 88%, Sentiment POSITIVE, Resolution RESOLVED. Branch: deploy/fahad-demo (master untouched). 6 fixes in deploy/fahad-demo: (1) whisper_service.py — monkeypatched torch.load to force weights_only=False (PyTorch 2.7 breaking change; must use direct kwarg assignment not setdefault — lightning_fabric explicitly passes the kwarg so setdefault is a no-op). (2) Dockerfile.gpu.blackwell — CT2 4.4.0→4.8.0 (cuDNN 8→9 compatibility for CUDA 12.8 Blackwell base image), removed cuDNN 8 LD_LIBRARY_PATH workaround. (3) calls.py — cached tenant_id before DB ops (MissingGreenlet on expired ORM after rollback) + docker cp clean file fix for Notepad TabError. (4) auth.py — added set_config platform_bypass before user SELECT — pre-existing RLS bug: RLS filtered all users at login since no tenant context exists before login; only surfaced after full container restart cleared cached DB session state. (5) alembic 20260621_idempotency — removed uq_calls_tenant_audio_path creation (blocks demo re-uploads); constraint also dropped live. (6) infra/.env — updated GROQ_API_KEY (revoked key); .env silently overrides .env.prod when both present in docker compose. Remote access: Tailscale (Adeen in Fahad tailnet, 100.118.221.10), WSL2 SSH + Windows portproxy port 22, Zed remote SSH. Public URL: http://callquality.giize.com (Dynu DDNS). Next: ROI reporting module, then Agentic AI assistant. VPS eval: OVHcloud Beauharnois + RunPod GPU for production. Handoff: 63_Session_Handoff_2026-06-25.md. Postmortem: 64_Fahad_Demo_Deployment_Postmortem.md.

2026-06-23 — Fixed fresh-DB Alembic bootstrap. `alembic upgrade head` now builds the full schema from an empty database in one command — no more `create_all` + `stamp 20260526_platform_rls_bypass` + `upgrade head` workaround. Root cause: the chain root `20260501_create_tenants` only created `tenants`; `20260501_add_tenant_id` then `ALTER`ed users/agents/calls/call_metrics/sentiment_timeline which were never created by Alembic (they came from `02_Database_Schema.sql` mounted into postgres `docker-entrypoint-initdb.d`). Squashed migrations 1–8 (create_tenants → add_tenant_id → enable_rls → force_rls → add_agent_sync_columns → agent_identity → indexes_and_constraints → platform_rls_bypass) into one new base migration `20260501_base_schema` (`down_revision=None`): pgcrypto ext, all 6 tables in exact historical column order, all FKs (CASCADE), CHECKs (`ck_tenants_plan_tier`, `users_role_check` 4-role, calls score/status), all indexes incl. partial `agents_external_id_tenant_idx`, `uq_call_metrics_call_id`, RLS ENABLE+FORCE on 5 tenant tables, and `*_tenant_isolation` policies in final platform_bypass form. Re-pointed `20260621_idempotency.down_revision` → base (it drops `uq_call_metrics_call_id` and adds the composite uniques on top). Deleted the 8 squashed files + `02_Database_Schema.sql` + removed its init-script mount from `infra/docker-compose.yml`. VERIFIED: ground-truth = original 9-migration chain run on top of `02` schema; pg_dump --schema-only of fresh `upgrade head` is byte-identical to ground truth (tables/columns/constraints/indexes/RLS policies/FORCE). Round-trip upgrade↔downgrade↔base↔upgrade clean. `reset_and_seed.py` seeds 200 calls successfully. Also fixed a pre-existing seed bug: `reset_and_seed.py:140` `bcrypt.hashpw(b"platform1234".encode(), ...)` → `.encode()` on a bytes literal (AttributeError), now `b"platform1234"`. pytest: 24 pass; 9 non-pass (6 async-loop fixture-scoping errors, write_scores arg-count, login/webhook) are pre-existing baseline — suite uses `create_all` from unmodified `orm.py`, never touches Alembic. CLAUDE.md Database section updated. gitnexus detect_changes: only `reset_and_seed.py:main` is a touched symbol; migrations/SQL/compose are not call-graph symbols.

2026-06-22 (session 5) — L4/L5 fixes + repo cleanup. Two commits pushed. `4b3a84a`: L4 — `@field_validator("coaching_summary")` added to `InferenceResult` (llm_client.py) asserting `len(v.strip()) > 0`; raises `ValueError` on empty string, caught by `run_inference` retry loop. L5 — `issue_category: str` changed to `Literal["billing_dispute", "technical_support", "account_access", "service_cancellation", "upgrade_request", "refund_request", "delivery_issue", "password_reset", "plan_change", "complaint"]`; Pydantic v2 validates on parse, hallucinated categories raise `ValueError` and trigger retry. `Literal` added to `from typing import` in llm_client.py. GitNexus impact: LOW, d=1 caller is `tasks.py`. detect_changes: `InferenceResult` touched, `_call_provider` and `validate_sentiment` line-drift only, 1 affected process (`run_groq_inference → _call_provider`). `95b3b5b`: cleanup — deleted 13 `.agents/skills/` plugin files (cavecrew, caveman-compress, caveman-stats, caveman), added `ui-ux-pro-max/` to `.gitignore`, parameterized MinIO image tags in `docker-compose.app.yml` from hardcoded to `${MINIO_IMAGE_TAG}` (confirmed present in `.env.example`). KNOWN_ISSUES.md written to vault. Talk balance formula verified PASS — code matches invariant. L6 deferred (external_agent_id max-length, no crash risk at demo scale).

2026-06-22 (session 4) — L1/L2/L3 low-severity fixes pushed as `eff0683`. L1: `expires_in` in both `login` and `register` now reads `settings.jwt_expiry_seconds` instead of hardcoded `28800`; `from app.config import settings` added to auth.py imports. L2a: email format validation added to `register` (auth.py) and `invite_user` (users.py) — `re.match(r"[^@\s]+@[^@\s]+\.[^@\s]+", ...)` before the DB query; returns 422 VALIDATION_ERROR on invalid format; `import re` added to users.py. L2b (cross-tenant oracle at users.py:112) was already fixed — `invite_user` had `User.tenant_id == current_user.tenant_id` in the email uniqueness check, no change needed. L3: three `== True` comparisons replaced with `.is_(True)` in SQLAlchemy filter expressions — `Agent.is_active` in `list_agents` (agents.py:171), `Call.resolved` in `get_agent_scores` (agents.py:240), `Agent.is_active` in `assign_agent` (calls.py:321). GitNexus impact: all targets LOW, 0 upstream callers. detect_changes: 6 symbols (login, register, invite_user, list_agents, get_agent_scores, assign_agent), 7 affected processes (all HTTP request flows).

2026-06-22 (session 3) — M4 prompt injection fix + duplicate Call row dedup pushed as `9aa70a5`. M4: added `sanitize_transcript(text: str) -> str` to `llm_client.py` using a targeted regex (`_UNSAFE_TAG_PATTERN`) that strips `<transcript>`, `</transcript>`, `<system>`, `</system>`, `<prompt>`, `</prompt>`, and related LLM-context injection tags from the transcript before `INFERENCE_PROMPT.format()` interpolation; called inside `run_inference()` after the empty-string guard and after pii_redacted gate (enforced at task level). Dedup: `upload_call` in `calls.py` now derives filename from `hashlib.sha256(file_bytes).hexdigest()[:16]` + original filename instead of `uuid.uuid4().hex` — same file content always produces the same `minio_audio_path`; `db.add(call)/await db.commit()` wrapped in `try/except IntegrityError` that rolls back, re-sets `app.current_tenant` GUC (required for RLS after new autobegin transaction), queries the existing Call row by `(tenant_id, minio_audio_path)`, and returns HTTP 200 with the existing call's data — no second pipeline chain fired. `hashlib`, `IntegrityError`, and `text` added to imports. GitNexus impact: both targets LOW. detect_changes: 2 logic-changed symbols (`upload_call`, `run_inference`) + line-drift on adjacent InferenceResult properties.

2026-06-22 (session 2) — Five code-review findings fixed and pushed as `b331932`. H3: re-set tenant GUC after `db.commit()` before `db.refresh()` in all four affected routes — `invite_user` (users.py), `create_agent`, `update_agent`, `deactivate_agent` (agents.py); `SET LOCAL` is transaction-local so GUC must be re-issued after each commit. Also added `text` import to users.py. M1: `@limiter.limit("5/minute")` added to `export_call_pdf` (reports.py) — matches pattern from `/auth/login`. M2: `create_tenant` (platform.py) switched to `get_db_platform` dependency; `get_db_platform` wraps entire request in `session.begin()` with `SET LOCAL app.platform_bypass = 'true'` already set, so the manual `async with db.begin()` + redundant SET LOCAL were removed and `db.refresh(tenant)` now runs inside the same open transaction with bypass active; `get_db` import removed. M3: `WHISPER_MODEL` hardcoded to `large-v2` in `docker-compose.gpu.yml` (was `${WHISPER_MODEL:-large-v2}`) and `docker-compose.prod.yml` (was `${WHISPER_MODEL}` with no fallback). M5: `_remap_speakers` (whisper_service.py) now assigns AGENT to the speaker with the longest total speaking duration across all segments rather than first appearance — more robust in professional call center context where agents consistently speak more. `deactivate_agent` was an additional H3 site not in the brief; fixed alongside the three specified. GitNexus impact: all targets LOW. detect_changes: 7 symbols, 9 files.

2026-06-22 — Full backend code review + infra pivot + 2 HIGH fixes pushed to master. Session opened by completing 5 dead code deletions (download_file, ConnectionManager.send, redis_password field, test_phase23.py, update_passwords.py — all confirmed 0 callers via GitNexus, committed `3262912`). Full backend triage followed: no CRITICAL findings; HIGH: H1 (`upload_audio` key included bucket name as prefix — all presigned URLs 404'd; fixed by removing prefix so put_object/download_file/presigned URL all reference `Key={agent_id}/{date_path}/{filename}`), H2 (`chain().apply_async()` dispatched after `db.commit()` with no error handling — Redis outage would orphan calls in "pending" forever; fixed in both ingestion paths with try/except + status="failed" + commit), H3 (`get_db_with_tenant` GUC lost after commit, `db.refresh()` in users.py + agents.py runs outside RLS context — NOT YET FIXED); MEDIUM: M1 Playwright DoS, M2 wrong db dep in create_tenant, M3 WHISPER_MODEL default, M4 prompt injection, M5 speaker heuristic; LOW: 6 minor issues. H1 + H2 fixed and pushed as `c5d8452`. Infrastructure decision: Azure abandoned for now (quota delays, cost) — demo runs on Fahad's Windows laptop (RTX 5060 8GB VRAM / 64GB RAM) via Dynamic DNS; VPS at first paying customer (RunPod or OVHcloud). RunPod Canada region claim logged as unverified in Lessons vault. Handoff: 62_Session_Handoff_2026-06-22.md.

2026-06-21 (session 2) — Pipeline idempotency audit + 3 fixes. All Celery tasks run with `task_acks_late=True` + `task_reject_on_worker_lost=True` meaning any task can be redelivered on worker crash. Found and fixed 3 gaps: (1) `write_scores` HIGH — DELETE+INSERT replaced with `ON CONFLICT DO UPDATE` upserts for `call_metrics` and `sentiment_timeline`; new migration `20260621_idempotency` adds `uq_call_metrics_call_tenant (call_id, tenant_id)`, `uq_sentiment_timeline_point (call_id, tenant_id, timestamp_seconds)`, `uq_calls_tenant_audio_path (tenant_id, minio_audio_path)` — migration applied cleanly against postgres:16-alpine, idempotency test passed (2 runs → 1 row in each table, values updated not duplicated). (2) `extract_agent_identity` MEDIUM — early-return guard (`call.agent_id is not None`) moved before LLM call so retries skip Groq round-trip entirely. (3) `minio_event` MEDIUM — duplicate webhook delivery now caught by `IntegrityError` on the new `uq_calls_tenant_audio_path` constraint, returns HTTP 200 without firing Celery chain twice. GitNexus impact: all 3 targets LOW risk, 0 callers. detect_changes: 3 files, 0 affected processes. NOTE: migration chain cannot run from scratch on a fresh DB — `20260501_create_tenants` only creates `tenants` table; all other tables were created outside Alembic before migrations started. Always run `alembic upgrade head` inside the `cq_api` container against the live DB, never on a blank instance.

2026-06-21 — Fixed hardcoded `device="cpu"` in `transcribe_and_diarize` (whisper_service.py line 60). Pyannote diarization was pinned to CPU regardless of environment — dev workaround for 8GB RTX 3060 Ti that was never updated for the T4 (16GB VRAM). Changed to `diarize_device = os.environ.get("DIARIZE_DEVICE", device)` so it inherits `WHISPER_DEVICE` by default (cuda in prod). Committed as `2bc6e6f`, pushed to master. GitNexus impact: LOW, 1 caller (`run_whisperx`). Full Docker integration test with real audio (`infra/test_call.wav`) running now against `cq-worker-gpu:test`. KNOWN RISK: `Dockerfile.gpu` pins `numpy<2` but `pyannote-core 6.0.1` and `pyannote-metrics 4.1` both require `numpy>=2.0` — pip flagged this as a dependency conflict during build (build succeeded but conflict is unresolved at runtime). If the integration test fails or produces incorrect diarization results, check numpy version inside container FIRST before debugging anything else (`python -c "import numpy; print(numpy.__version__)"`).

2026-06-20 (session 3) — Codex backend + frontend review: 26 findings fixed across 13 files. Backend (session 2): `get_db_with_tenant` wrapped `SET LOCAL` inside `async with db.begin()` — transaction committed before yield, RLS completely unenforced for all tenant endpoints; fixed by removing `begin()` wrapper (SQLAlchemy 2.0 autobegin keeps transaction open for the endpoint's lifetime). `create_tenant` had `db.add()` before `begin()` block — moved inside. `_on_task_failure` read `args[0]` as call_id for all tasks but chained tasks (`.s()`) have `prev_result` at `args[0]`; fixed by branching on `sender.name`. MinIO hardcoded credentials replaced with `os.environ["MINIO_ACCESS_KEY/SECRET_KEY"]` (KeyError if missing). HTML injection in PDF reports via `html.escape()`. `error_response()` helper added to schemas, all routers now return correct HTTP codes (401/400/409/404). FastAPI docs disabled in prod. Frontend (this session): WebSocket ghost reconnect after logout fixed with `shouldReconnect` ref set false before `ws.close()` in cleanup; `UploadCall.tsx` 50 MB file size guard in handleDrop/handleUpload/onChange; `/register` now redirects authenticated users via `RedirectIfAuth`; `tenant_id` added to `UserOut` (backend schemas + auth.py 3 call sites) and `User` interface + `LoginResponse` type (frontend); `AgentUpdateRequest.external_id` added to `api.ts`, `AgentManagement.tsx` type-intersection workaround removed; sequential 3-page waterfall in `Agents.tsx` + `Overview.tsx` replaced with parallel `Promise.all` (cap 5 pages); hardcoded purple `rgba(86,69,212,0.04)` → teal `rgba(0,169,157,0.04)`; `CONFIDENCE_STYLE` hex values (`#047857`, `#ecfdf5`, `#b45309`, `#fffbeb`) → CSS design tokens; blob export typed as `client.post<Blob>`, `URL.createObjectURL(res.data)` directly; `baseURL` reads `VITE_API_BASE_URL` env var; `main.tsx` non-null assertion → null check with throw; empty `catch {}` → `console.warn`; `map.get()!` non-null assertion → safe bucket check. Committed as 2 groups, pushed to GitHub.

2026-06-20 (session 2) — Security audit (OWASP Top 10 + Azure) complete. 5 fixes applied: (1) `platform.py` `list_tenants` + `create_tenant` missing `get_db_platform` RLS bypass — HIGH; (2) `minio_event.py` timing attack on bearer token comparison → `hmac.compare_digest` — MEDIUM; (3) `main.py` FastAPI `/docs`+`/redoc` disabled in prod — MEDIUM; (4) `llm_client.py` LLM01 prompt injection — XML delimiters around transcript + SYSTEM_PROMPT hardening — MEDIUM; (5) `nginx.conf` security headers added (X-Frame-Options, X-Content-Type-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy, server_tokens off) — MEDIUM. Deferred (your call): password min 12 chars, email enumeration on register, HTTPS, Azure Key Vault. Codex quality review prompt written. All 6 changed files staged for commit.

2026-06-20 — Architecture corrected: B4ms CPU-only (pipeline blocker) replaced with Option C — B4ms app tier (always-on) + NC4as_T4_v3 T4 GPU worker (on-demand/Spot, ~$130-180/mo). Fully cloud, no local GPU, no SSH tunnels. Split compose done: `docker-compose.app.yml` (B4ms) + `docker-compose.gpu.yml` (NC4as_T4_v3, Dockerfile.gpu, CUDA 12.1). NSG runbook + GPU .env template created. `.env.prod` fixed: REDIS_PASSWORD, MINIO_WEBHOOK_SECRET, CORS_ORIGINS added. Vault corrected v1.7→v1.9. CLAUDE.md fixed (7 issues). 11_Azure_Deployment.md fully rewritten for split architecture.

2026-06-19 (session 2) — Final pre-Azure cleanup. Deleted batch_agent/ (replaced by MinIO webhook). Added infra/.env.example. Full graphify semantic extraction via OpenRouter DeepSeek v3: 1,061 nodes / 2,320 edges / 63 named communities. Registered openrouter-ds custom provider in ~/.graphify/providers.json. Fixed Obsidian MCP vault path in ~/.claude.json (was /obsidian-vault, now /second-brain/cqa-vault). Vault fully updated via MCP. Repo pushed — SHIP READY.

2026-06-19 — Azure predeployment audit + all blockers fixed. Created Dockerfile.cpu, frontend/Dockerfile, infra/nginx.conf, infra/docker-compose.azure.yml, 20260526_platform_rls_bypass migration. Fixed SET LOCAL f-strings (set_config() bind params), notify_websocket Redis URL fallback removed. Platform_bypass migration uses remote's better pattern (modifies existing tenant_isolation policies to add OR bypass clause, not a separate policy). Resolved rebase conflicts with remote's prod Dockerfile + nginx. Pushed to GitHub master. Architecture: hybrid retired → single B4ms CPU-only self-contained. NEXT: az vm create + docker compose up. Playwright waste (M3) deferred to Phase 2.

2026-06 — Security P0 session: PyJWT migration (python-jose CVE-2024-33664 removed), Redis requirepass added, slowapi rate limiting, CORS env var, image pins, tenant-scoped DELETE in write_scores, RoleGuard on /tenants route. Platform admin: 5 pages live (overview, tenants, health, usage, call monitor) + PLATFORM_ADMIN UI via Antigravity. RLS bypass migration added (modifies tenant_isolation policies). design system changed to Waaqi GRC tokens (#00a99d primary, #0f1924 sidebar). React 18 → React 19. motion/react ^12.40.0 added.

2026-05-03 (session 2) — Architecture reviews done (DeepSeek/GLM/Kimi/Opus). Key findings: talk_balance formula wrong, pipeline not idempotent, Redis no AOF, pricing should be $15-20 not $5-10, build ROI blind-spot report before new features, South Asia = relationship sales not self-serve. Workflow formalised: Claude=auditor+prompt writer, Codex/Copilot=code generator. Research report PDF generated (Perplexity+GPT+Gemini+Manus). Tenant isolation live (Demo+Acme). Vault updated: docs 41, 42, INVARIANTS, WORKFLOW, ROADMAP. Handoff: doc 42.

2026-05-03 — Phase 7 frontend complete + E2E verified (Sarah Chen auto-assigned). Pipeline order fix (identity before PII). Login fixed (FORCE RLS + Vite proxy). Full UI polish: Login shadow card, coloured agent avatars, Sidebar accent bar, drag-drop upload, Reports WS pill, CallDetail JSON→pill. _remap_speakers bug fixed. reset_and_seed.py rewritten for multi-tenancy. .wslconfig + TDR fix for PC crashes. Gemini tasked with finding real call audio. Handoff: doc 40.

2026-05-02 — Phase 7 backend complete. Migration 006 applied locally (agent_id nullable, needs_agent_review, agent_name_extracted, external_agent_id). extract_agent_identity task live. 5 bugs caught in review pre-run. C drive 0GB incident — hibernation disabled, caches cleared, full reboot required. Notion supervisor dashboard created. Frontend remaining: Needs Review badge, manual assign, upload form.

2026-04-29 — Sea-level dashboard live. UploadResponse + radar sentiment fix applied. GPT audit cross-verified. WhisperX debug system designed (doc 28).

2026-04-19 — UI audit complete. Sea-level dashboard live (6 panels). Agents page hardened. CallList/CallDetailPanel polished. PASS verdict. 2 major issues (non-blocking): Evaluated-at label, minCalls=5 filter.

2026-04-18 — Final session on Pro account. Built custom knowledge graph builder (scripts/build_graph.py). Outputs GRAPH_REPORT.md. Added STARTUP_HYBRID.md + STARTUP_LOCAL.md. PROMPTING_GUIDE.md + INVARIANTS.md for multi-LLM workflow.

2026-07-04 — Codebase cleanup complete. Router/service split, pipeline stages split, frontend decomp. c952b11 master. CT2 pinned 4.8.0. call-qa.tech domain acquired, Cloudflare Tunnel pending. Next: uv migration.
